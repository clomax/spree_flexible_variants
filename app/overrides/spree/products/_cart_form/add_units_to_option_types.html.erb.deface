<!-- insert_after 'span.variant-description' -->
<% units = variant.unit_amount %>
<% if @product.cut_from_whole? and units %>
  <span>(<%= units %> <%= units == 1 ? "unit" : "units" %>)</span>
<% end %>
